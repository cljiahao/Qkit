# App
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Shared-session SSO across *.merqo.io kits — leave unset in dev/preview
# (they don't run on merqo.io; setting this there breaks login instead of
# just failing to share it). Set to .merqo.io in Vercel Production only.
NEXT_PUBLIC_AUTH_COOKIE_DOMAIN=

# Merqo dashboard metrics endpoint — bearer secret for GET /api/merqo/metrics.
# Server-only. Must equal the value stored in merqo's products.metrics_secret.
MERQO_METRICS_SECRET=

# Guards POST /api/merqo/vendor-provision (merqo hub's one-click "Activate
# qkit" button) — a write capability, deliberately a DIFFERENT secret from
# MERQO_METRICS_SECRET above so a leak of one doesn't grant the other.
MERQO_PROVISION_SECRET=

# loopkit deployment URL — used by the order-status page to check whether a
# vendor has qkit-earn enabled and to build the "Earn a stamp" link. If
# unset, the order-status page fails closed (link never shows).
NEXT_PUBLIC_LOOPKIT_URL=

# paykit deployment URL — base for the /api/v1/* checkout API qkit's payment
# flow now calls (vendor PayNow config, checkout, claim, confirm). Falls back
# to https://paykit-sg.vercel.app if unset. See src/lib/paykit/client.ts.
NEXT_PUBLIC_PAYKIT_URL=

# Bearer secret for paykit's /api/v1/* API — sent as
# `Authorization: Bearer qkit:<secret>`. Must equal the value paykit's
# kit_api_keys table stores for kit_slug='qkit' (minted via paykit's
# scripts/create-kit-key.mjs). Server-only. NOT YET MINTED for production —
# leave unset; src/lib/paykit/client.ts degrades to a clear per-request error
# (not a build failure) when this is empty.
PAYKIT_KIT_SECRET=

# Customer + vendor Telegram connect: qkit calling INTO merqo's own shared
# Telegram bot — customer side (a "Get notified on Telegram" button on the
# order-status page, plus advanceOrder's ready-transition notification) and
# vendor side (placeOrder's new-order alert, Phase A2 — supersedes qkit's own
# now-retired bot). The first kit → merqo HTTP direction in this ecosystem.
# MERQO_BASE_URL: merqo's deployment URL (falls back to
# https://merqo-sg.vercel.app if unset). MERQO_CUSTOMER_SECRET: bearer secret
# sent as `Authorization: Bearer <value>` to merqo's
# POST /api/merqo/customer-connect-token, POST /api/merqo/notify-customer, and
# POST /api/merqo/notify-vendor — must equal the value merqo's own
# MERQO_CUSTOMER_SECRET env var records. Server-only; unset locally is fine for
# Telegram — src/lib/merqo-customer-notify.ts fails closed (TelegramConnect
# renders null, notifyCustomer/notifyVendor no-op) rather than break anything.
#
# These same two vars ALSO gate the legal-acceptance flow (src/lib/legal-gate.ts
# + src/app/legal/accept): GET /api/merqo/legal-status (is this vendor current
# on terms/privacy?) and POST /api/merqo/legal-accept (record acceptance). That
# gate fails CLOSED the other way — with MERQO_CUSTOMER_SECRET unset, every
# signed-in vendor is bounced to /legal/accept and the accept action throws, so
# set it for any environment where vendors sign in.
MERQO_BASE_URL=
MERQO_CUSTOMER_SECRET=

# Bearer secret for POST /api/printkit/print-status — printkit calling INTO
# qkit with a print job's terminal status. A plain shared secret (no
# kit_slug prefix): this endpoint has exactly one caller (printkit), unlike
# paykit's PAYKIT_KIT_SECRET above. Must equal the value set for printkit's
# own QKIT_CALLBACK_SECRET env var. Server-only.
PRINTKIT_CALLBACK_SECRET=

# printkit deployment URL — base for the /api/v1/print-jobs API qkit calls
# to create a print job when an order is placed. No fallback URL: printkit
# has no live deployment yet. If unset, src/lib/printkit/client.ts fails
# closed (job creation silently skipped — placeOrder's own result is never
# affected). See src/lib/printkit/client.ts.
NEXT_PUBLIC_PRINTKIT_URL=

# Bearer secret for printkit's /api/v1/* API — sent as
# `Authorization: Bearer qkit:<secret>`. Must equal the value printkit's
# kit_api_keys table stores for kit_slug='qkit'. Server-only. If unset,
# src/lib/printkit/client.ts fails closed (job creation silently skipped)
# rather than a build failure.
PRINTKIT_KIT_SECRET=

# Google OAuth (read by the Supabase CLI when running `supabase start`, NOT by Next).
# Create an OAuth 2.0 Web client in Google Cloud Console; authorized redirect URI:
#   local: http://127.0.0.1:54321/auth/v1/callback
#   prod:  https://<project-ref>.supabase.co/auth/v1/callback
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=
