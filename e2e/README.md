# e2e

## Purpose

Playwright end-to-end smoke tests, run against a real local Supabase (not mocked) — covers what the mocked unit/component tests can't (RLS, the `proxy.ts` auth guard, the full order lifecycle).

## Contents

- `auth-guard.spec.ts`
- `customer-order.spec.ts`
- `order-code.spec.ts`

## Parent

[qkit](../README.md)
