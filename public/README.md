# public

## Purpose

Static assets served as-is by Next.js.

## Contents

- `seed/`
- `sw.js`

## Connectivity

`seed/` holds placeholder menu-item images used by demo/seed data; `sw.js` is the service worker registered by `ServiceWorkerRegistrar` (`src/components/service-worker-registrar.tsx`).

## Parent

[qkit](../README.md)
