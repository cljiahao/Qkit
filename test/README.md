# test

## Purpose

Vitest tests not colocated with their source — currently API route tests (mirroring `src/app/api/`'s structure) and one integration test.

## Contents

- `api/`
- `order-numbering.integration.test.ts`
- `setup.ts`

## Connectivity

`api/` mirrors `src/app/api/`'s folder structure one-for-one, one test file per route handler.

## Parent

[qkit](../README.md)
